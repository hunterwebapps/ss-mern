import React from 'react';
import PropTypes from 'prop-types';

const Home = ({ }) => (
    <React.Fragment>
    </React.Fragment>
);

Home.propTypes = {
    
}

Home.displayName = 'Home Page';

export default Home;

